# 飞书首期后端验收清单

状态：2026-09-14，完整 Core 已达到 2500 通过、0 失败、8 项可选集成／夹具跳过；最后的基线失败定位为旧测试时区并修正。见 [最新验证](asyncjob-timezone-correction-20260914.md)。勾选表示自动化和代码证据，不代表真实飞书送达或目录外接入已验收。

## C0 范围与阶段

- [x] 首期包括飞书全部后端通知能力，后续只增加其他渠道。
- [x] 通知分支以官方 main `44c4e038` 为基线，保留的新增内容均在 backend。
- [x] spec/tasks/checklist 已按用户批准计划同步，无暂停、摘要、全文、产物或多目标的分期裁剪。
- [x] 阶段一新增测试只调用真实业务代码、模拟外部边界，当时未编写生产逻辑或迁移 SQL；随后获批进入实现。
- [x] 本批接口形状、错误码、需求映射及验证结果已整理为可 Review 文档。
- [x] 2026-09-10 用户明确回复“批准”，允许进入生产实现。

## C1 全局设置与任务配置（R1、R2）

- [x] 默认总开关开启；成功/失败摘要开启；暂停摘要关闭；飞书须显式启用。
- [x] 全局默认只复制到新任务；已有规则不变；恢复默认复制当前规则并返回新版本。
- [x] 各状态的内容模式独立保存，关闭事件/渠道保留选择。
- [x] 无启用渠道可保存全部事件关闭；有启用渠道必须保留事件；默认事件至少一个。
- [x] 多账号多目标全部持久化；缺字段、非法枚举、重复目标、错误类型和过长输入拒绝。
- [x] 保存后读取/重建路由一致；持久化/网关校验失败不假装成功。
- [x] 两个相同版本并发写入仅一个成功，另一项 409；原任务执行保留规则快照。
- [x] 关闭影响只包含当前用户执行；新增影响使旧确认失效；关闭和重开保留配置。
- [x] 原创建、批量创建可带规则；原编辑省略通知字段不清空配置。

## C2 账号、接收对象与权限（R3）

- [x] 同平台同名账号保持不同 ID；支持多个账号和接收对象，返回头像、状态、时间及引用信息。
- [x] 群聊是可访问且有发送权限的群；个人接收者身份经过验证，任意用户 ID 被拒绝。
- [x] 查询、保存、发送与重试均验证所有权；跨用户计划、任务、账号及历史不可访问。
- [x] 缺权限、群失效、账号断开有稳定安全原因；不自动改绑。
- [x] 飞书注册请求包含群读取权限，旧账号可补授权而保留引用。
- [x] 断开保留账号、规则与历史；同身份重连保持 ID，不同身份重连拒绝。
- [x] 注册失败孤立账号仍可清理，不误删已连接账号。
- [x] 当前连接过期、刷新、取消及网络/授权失败的基础回归在两种数据库上通过。

## C3 事件、摘要、全文与产物（R4）

- [x] 成功事件与已持久化结果一致；等待或活跃执行不能被错误收尾为成功。
- [x] 输出持久化失败不产生成功；事件插入失败回滚相关状态；外层事务回滚不留事件。
- [x] 失败安全原因、暂停原因及待处理事项正确，暂停不发送未完成的结果。
- [x] 可恢复等待/停止通知受暂停开关控制；重复暂停一次，恢复再暂停是新事件。
- [x] 计划停用、等待上游、彻底取消不映射为成功/失败/暂停通知。
- [x] 标准调度、立即执行、批量创建、依赖恢复的规则和事件均正确。
- [x] 有来源的有效摘要复用；正文截取不当作有效摘要；用户所选纯模型禁止工具调用。
- [x] 长文分段归纳覆盖所有内容，摘要生成有超时并持久化，跨目标/重试复用。
- [x] 摘要失败先发状态和“摘要暂不可用”，记录部分成功；补发只补摘要，不重跑任务。
- [x] 全文分卡不丢段、不重复、不乱序；中文、emoji、JSON 转义计入序列化预算。
- [x] 图片和文件原生发送；空文件/超限/不可用产物列明名称、安全原因和返回 LazyMind 提示。
- [x] 不能放入卡片的内容转文件；单个失败产物不阻断后续产物，保留部分成功记录。

## C4 投递、重试与竞态（R5）

- [x] Core 与网关的持久化交接无需入站消息；入队与实际发送状态区分。
- [x] 按执行/事件实例/账号/目标去重，重启或不同传输 ID 不绕过去重。
- [x] 已成功分片有持久回执；中断恢复和人工重试不重发成功部分。
- [x] 平台响应丢失重用稳定 UUID；超过 1 小时去重窗口仍不确定时记录 unknown。
- [x] unknown 停止自动发送，人工重试必须确认可能重复。
- [x] 同一失败记录并发重试仅一次入队；保留内容、目标、retry_of；不改变任务执行次数。
- [x] 每片重新检查总开关、账号和权限；Core 无法确认时暂缓；关闭使未发部分失效。
- [x] 关闭后重开不恢复旧队列；已进入平台请求的分片可能完成，其余不发送。
- [x] 单目标失败不影响其他目标；任务成功/失败状态不依赖通知服务可用性。
- [x] Core 重启恢复待交接通知；网关接受后丢失响应不会重复生成投递。

## C5 错误、契约与数值边界

具体接口及已实施编号见 [测试契约](test-contract.md)；新增 SDK 明确拒绝错误分类已按确认实施，先前未知请求继续保留重复风险。

- [x] Core 配置非法 400/2002701、版本冲突 409/2002702、渠道不支持 400/2002705、影响变化 409/2002706、校验服务不可用 503/2002710 与测试一致。
- [x] 网关公开错误为稳定字符串 code、安全 message、retryable、request_id；结构校验 422，跨用户资源 404，内部认证缺失 401。
- [x] 新接口进入后端集中权限提取和 OpenAPI，未修改目录外契约/i18n 产物。
- [x] 卡片序列化不超过 28 × 1024 字节；平台拒绝可安全观察。
- [x] 图片不超过 10 × 1024² 字节、文件不超过 30 × 1024² 字节，边界上下和空文件独立验证。
- [x] 现有 SDK 卡片/图片/文件请求的 60 秒超时和稳定 UUID 边界测试通过。
- [x] 可控时钟验证现有 30 秒续租和租约丢失隔离行为。
- [x] 任务通知全链路使用 120 秒租约、最多自动尝试 5 次，遵守平台限流和 Retry-After。

## C6 双数据库、迁移与回归

- [x] Core API/状态测试分别用文件 SQLite、真实 PostgreSQL 执行，结果单独报告。
- [x] 网关及 E2E 两种数据库均可建立独立测试环境，无环境跳过。
- [x] 当前 main 的 PostgreSQL dev/aggregate 基础迁移验证通过。
- [x] 当前 main 的 SQLite dev/aggregate、新建/旧数据升级基础验证通过。
- [x] 通知新 SQL migration 的 up/down/up、旧数据保留及不补发历史验证通过（2026-09-11，PostgreSQL 与文件 SQLite）。
- [x] 通知 dev/aggregate 最终 schema 相同，aggregate down 恢复之前版本；已合并 dev SQL 不变（2026-09-11，两库通过）。
- [x] 已批准批次中通知配置、事件、回执的事务/并发断言在两种数据库通过；新增 SDK 拒绝场景及恢复检查也已通过。
- [x] 原飞书/微信会话回复、已有凭据保护、所选 Core 调度/任务中心回归通过。

## C7 主流程与交付

- [x] 模拟外部边界的完整链路：配置计划 → 执行 → 结果持久化 → 平台发送调用 → 查询正确事件/目标/分片；独立进程双库 E2E 22 项通过。
- [x] 故障链路：投递失败 → 原任务保留 → 单独重试 → 不重跑任务且只补失败部分。
- [ ] 真实测试账号、接收对象和消息获明确授权后验证飞书实际可见送达。
- [x] backend 内提供内部 URL/Token、补授权、OpenAPI/权限/i18n 发布的目录外交接说明。 见 [backend-handoff.md](backend-handoff.md)。
- [x] 本阶段报告分别列出自动化通过／失败、真实送达未执行、目录外部署未接入及残余风险；不宣称最终完成。

## 2026-09-11 补充 Review

- [x] 获批端到端夹具列名修正已应用，22 项双数据库 E2E 通过；未真实外发。
- [x] 新增真实工作流停止/恢复、引擎暂停/失败、通知写入失败回滚及彻底取消测试；两库分别 3 项预期失败、1 项通过。
- [x] 用户已批准 workflow-review-proposal.patch 中既有停止状态断言与迁移目录计数修正，已应用。
- [x] 补充测试已获批，工作流事务与通知接入已实施；两库各 73 项当前回归包含实际停止／恢复测试。

## 2026-09-13 SDK 边界与最终集成 Review

- [x] 新增实际 SDK 拒绝结果的两类测试，文件 SQLite／PostgreSQL 共 4 项预期失败；无环境跳过，后续未达到断言不记通过。
- [x] 既有两处数据库夹具和一处文案校验适配已整理为可应用补丁；用户确认后已应用，保持原断言并增加 backend 翻译校验；补丁反向检查通过。
- [x] 静态检查通过，13 条公开权限注册及网关 9 个通知相关路径 schema 已核对，改动只在 backend。
- [x] 用户批准新增 SDK 测试和 integration-review-proposal.patch。
- [x] 应用获批测试适配，修复明确 SDK 拒绝分类；拒绝产物记失败、有具名提示且不阻塞后续产物。
- [x] 新旧受影响回归复验通过；原基线异步任务失败已修正为测试 UTC 夹具，完整 Core 0 失败；可选集成跳过继续披露。
- [ ] 获得指定账号／接收对象／消息授权后执行真实飞书验收；目录外部署由对应接入范围处理。

SDK 缺口已修复并验证，C7 故障链路具有 E2E 与真实 SDK 结果转换的模拟验证证据。真实平台送达、目录外部署仍保留未完成；Core 原基线失败已修正，8 项可选集成／夹具跳过仍独立报告。

- [x] 2026-09-14：原唯一 Core 失败根因明确，测试夹具时区修正；文件 SQLite／PostgreSQL 的 asyncjob 模块各 27 通过，完整 Core 2500 通过、0 失败、8 跳过。
