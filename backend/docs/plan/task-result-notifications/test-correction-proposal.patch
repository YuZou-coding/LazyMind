--- a/backend/core/notification_api_test.go
+++ b/backend/core/notification_api_test.go
@@ -545,7 +545,7 @@
 		"/schedules/{schedule_id}/notification-rule": "put", "/schedules/{schedule_id}/notification-rule:reset": "post",
 		"/task-center/tasks/{task_id}/notifications": "get", "/task-center/notifications/{notification_id}:retry": "post",
 	} {
-		if len(spec.Paths[path][method]) == 0 {
+		if len(spec.Paths[apiPrefix+path][method]) == 0 {
 			t.Errorf("missing OpenAPI operation %s %s", method, path)
 		}
 	}
--- a/backend/core/scheduler/notification_lifecycle_test.go
+++ b/backend/core/scheduler/notification_lifecycle_test.go
@@ -11,6 +11,7 @@
 	"time"
 
 	"gorm.io/gorm"
+	"lazymind/core/common"
 	"lazymind/core/common/orm"
 )
 
@@ -45,7 +46,7 @@
 			w.WriteHeader(400)
 			return
 		}
-		if err := db.Create(&orm.ChatHistory{ID: "history-" + request.ConversationID, Seq: 1, ConversationID: request.ConversationID, Result: "已持久化的最终结论"}).Error; err != nil {
+		if err := db.Create(&orm.ChatHistory{ID: common.GeneratePrefixedID("hist_", 36), Seq: 1, ConversationID: request.ConversationID, Result: "已持久化的最终结论"}).Error; err != nil {
 			w.WriteHeader(500)
 			return
 		}
--- a/backend/core/scheduler/automation_test.go
+++ b/backend/core/scheduler/automation_test.go
@@ -19,7 +19,7 @@
 	if err != nil {
 		t.Fatal(err)
 	}
-	if err := db.AutoMigrate(&orm.UserSchedule{}, &orm.ScheduleDependency{}, &orm.TaskCenterTask{}, &orm.ChatHistory{}, &orm.ConversationArtifact{}, &orm.TaskRunOutput{}, &orm.TaskRunInput{}); err != nil {
+	if err := db.AutoMigrate(&orm.UserSchedule{}, &orm.ScheduleDependency{}, &orm.TaskCenterTask{}, &orm.ChatHistory{}, &orm.ConversationArtifact{}, &orm.TaskRunOutput{}, &orm.TaskRunInput{}, &orm.SubAgentTask{}, &orm.SubAgentArtifact{}); err != nil {
 		t.Fatal(err)
 	}
 	return db
